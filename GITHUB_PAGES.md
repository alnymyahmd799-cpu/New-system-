# رفع تطبيق «البركة للتوزيع» إلى GitHub Pages

هذه الحزمة **تطبيق ويب ثابت** ولا تحتاج إلى Node.js أو قاعدة بيانات مستضافة أو خادم خاص. اجعل ملف `index.html` في جذر المستودع، مع إبقاء بقية الملفات كما هي.

## الخطوات من الهاتف أو الكمبيوتر

1. أنشئ مستودعاً جديداً في GitHub، مثلاً `albaraka-distribution`.
2. ارفع **محتويات** هذا المجلد إلى جذر المستودع، ولا ترفع المجلد نفسه داخل مجلد إضافي.
3. من GitHub، افتح `Settings` ثم `Pages`.
4. ضمن **Build and deployment** اختر `Deploy from a branch`.
5. اختر فرع `main` والمجلد `/(root)`، ثم اضغط `Save`.
6. انتظر إلى أن يعرض GitHub رابط الموقع، وعادةً يكون بالشكل: `https://اسمك.github.io/albaraka-distribution/`.

## الملفات المطلوبة

| الملف أو المجلد | الغرض |
|---|---|
| `index.html` | نقطة تشغيل التطبيق. |
| `styles.css` و`responsive.css` | الهوية البصرية والتجاوب. |
| `js/app.js` | تشغيل الواجهة والتنقل وتسجيل الطلبات. |
| `js/data.js` | التخزين المحلي والحسابات. |
| `js/firebase-config.js` و`js/firebase-service.js` | مزامنة Firebase Realtime Database. |
| `js/print-service.js` | طباعة الفاتورة أو مشاركتها من المتصفح. |
| `.nojekyll` | يمنع GitHub Pages من معالجة الملفات بأسلوب Jekyll. |

## تنبيه Firebase

إعداد Firebase مضمّن لتشغيل تطبيق الويب في المتصفح، لكن يجب ضبط **Realtime Database Rules** بحيث تسمح فقط للمستخدمين المصرح لهم عند النشر الفعلي. لا تستخدم قواعد تسمح بالكتابة العامة على الإنترنت في تطبيق إنتاجي.

## الطباعة

من صفحة **الطلبات**، استخدم زر «طباعة» لفتح فاتورة HTML ثم اختر الطابعة أو «حفظ بصيغة PDF» من مربع حوار المتصفح.

